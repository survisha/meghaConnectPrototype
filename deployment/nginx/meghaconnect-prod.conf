server {
    listen 80;
    server_name meghaconnect.com www.meghaconnect.com;

    root /var/www/meghaconnect-prod;
    index index.html;
    client_max_body_size 25M;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/v1/ {
        proxy_pass http://127.0.0.1:8082/api/v1/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $http_authorization;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-Id $http_x_request_id;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 120s;
    }

    location /api/actuator/health {
        proxy_pass http://127.0.0.1:8082/api/actuator/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Authorization $http_authorization;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS version after Certbot/SSL certificate is available:
#
# server {
#     listen 443 ssl http2;
#     server_name meghaconnect.com www.meghaconnect.com;
#
#     ssl_certificate /etc/letsencrypt/live/www.meghaconnect.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/www.meghaconnect.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     root /var/www/meghaconnect-prod;
#     index index.html;
#     client_max_body_size 25M;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/v1/ {
#         proxy_pass http://127.0.0.1:8082/api/v1/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header Authorization $http_authorization;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
